# Memo Rust course

- cargo check: see if the code compiles
- cargo build: build the binary
- cargo build --release for faster runtime but slower
- cargo run: cargo build + exec actual binary produced in ./target/debug (./target/release if using --release)
